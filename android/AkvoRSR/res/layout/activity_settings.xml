<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android"
    android:title="Settings">

  <PreferenceCategory
     android:title="Options">

     <CheckBoxPreference
       android:key="setting_debug"
       android:title="Debug"
       android:summary="Show extra debug info"
       android:defaultValue="false"
       />
    <CheckBoxPreference
       android:key="setting_send_images"
       android:title="Send images"
       android:summary="Embeds images in uploads"
       android:defaultValue="true"
       />
    <!--
    <CheckBoxPreference
       android:key="setting_prefetch_images"
       android:title="Fetch images early"
       android:summary="Fetches all thumbnails dring a refresh. This may take a long time, but they are then available when offline."
       android:defaultValue="true"
       />
    -->
    <Preference
       android:key="data_host"
	   android:title="Data server"
    	/>
    
  </PreferenceCategory>

<!--       
  <PreferenceCategory
     android:title="Selection">
    <ListPreference
        android:key="pref_type"
        android:title="Type"
        android:summary="Select item from array"
        android:entries="@array/types"
        android:entryValues="@array/types_values"
        android:defaultValue="1"
        />

    <EditTextPreference
        android:key="pref_text"
        android:title="Input text"
        android:summary="Tap to enter some text"
        android:dialogTitle="Enter text"
    />
     
  </PreferenceCategory>
  <Preference
    android:title="Intent"
    android:summary="Open a webpage">

    <intent
      android:action="android.intent.action.VIEW"
      android:data="http://android-elements.blogspot.com/" />

  </Preference>
 -->
   
</PreferenceScreen>
